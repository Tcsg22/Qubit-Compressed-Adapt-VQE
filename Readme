#RLE-FED demo

##Installation
Run in the terminal

```sh
python -v venv rle-fed
source rle-fed/bin/activate
pip install -r requirements.txt
ipython kernel install --user --name rle_fed
```

##Example
Run python notebook `RLE-FED Algorithm.ipynb`

```sh
jupyter notebook --NotebookApp.kernel_manager_class=rle_fed RLE-FED Algorithm.ipynb
```
